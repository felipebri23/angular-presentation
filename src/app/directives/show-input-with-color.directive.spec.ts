import { ShowInputWithColorDirective } from './show-input-with-color.directive';

describe('ShowInputWithColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowInputWithColorDirective();
    expect(directive).toBeTruthy();
  });
});
